# =============================================================================
# WAPRO Network Mock - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values as needed.
# Set ENABLED=false to disable a mock service and use external/real service.
# =============================================================================

# -----------------------------------------------------------------------------
# GENERAL SETTINGS
# -----------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=prinet
NETWORK_SUBNET=172.28.0.0/16

# -----------------------------------------------------------------------------
# MSSQL WAPROMAG DATABASE
# -----------------------------------------------------------------------------
# Set to false to use external SQL Server instead of Docker container
MSSQL_ENABLED=true

# Connection settings (used by RPI server to connect)
# For Docker mock: use container name (mssql-wapromag)
# For external DB: use real hostname/IP (e.g., 192.168.1.100)
MSSQL_HOST=mssql-wapromag
MSSQL_PORT=1433
MSSQL_USER=sa
MSSQL_PASSWORD=WapromagPass123!
MSSQL_DATABASE=WAPROMAG_TEST

# Docker container settings (only used when MSSQL_ENABLED=true)
MSSQL_CONTAINER_NAME=wapromag-mssql
MSSQL_EXTERNAL_PORT=1433
MSSQL_SA_PASSWORD=WapromagPass123!

# -----------------------------------------------------------------------------
# RPI SERVER (Main Application)
# -----------------------------------------------------------------------------
RPI_ENABLED=true

# Server settings
RPI_CONTAINER_NAME=rpi-mock-server
NODE_ENV=development

# Port mapping (external:internal)
RPI_GUI_EXTERNAL_PORT=8082
RPI_GUI_INTERNAL_PORT=8080
RPI_API_EXTERNAL_PORT=8081
RPI_API_INTERNAL_PORT=8081

# -----------------------------------------------------------------------------
# ZEBRA PRINTER 1
# -----------------------------------------------------------------------------
# Set to false to use real ZEBRA printer instead of mock
ZEBRA_1_ENABLED=true

# Printer identification
ZEBRA_1_NAME=ZEBRA-001
ZEBRA_1_MODEL=ZT230

# Connection settings (used by RPI server to connect)
# For Docker mock: use container name (zebra-printer-1)
# For real printer: use real hostname/IP (e.g., 192.168.1.50)
ZEBRA_1_HOST=zebra-printer-1
ZEBRA_1_SOCKET_PORT=9100

# Docker container settings (only used when ZEBRA_1_ENABLED=true)
ZEBRA_1_CONTAINER_NAME=zebra-printer-1
ZEBRA_1_EXTERNAL_SOCKET_PORT=9100
ZEBRA_1_EXTERNAL_WEB_PORT=8091
ZEBRA_1_INTERNAL_WEB_PORT=8080

# -----------------------------------------------------------------------------
# ZEBRA PRINTER 2
# -----------------------------------------------------------------------------
# Set to false to use real ZEBRA printer instead of mock
ZEBRA_2_ENABLED=true

# Printer identification
ZEBRA_2_NAME=ZEBRA-002
ZEBRA_2_MODEL=ZT410

# Connection settings (used by RPI server to connect)
# For Docker mock: use container name (zebra-printer-2)
# For real printer: use real hostname/IP (e.g., 192.168.1.51)
ZEBRA_2_HOST=zebra-printer-2
ZEBRA_2_SOCKET_PORT=9100

# Docker container settings (only used when ZEBRA_2_ENABLED=true)
ZEBRA_2_CONTAINER_NAME=zebra-printer-2
ZEBRA_2_EXTERNAL_SOCKET_PORT=9101
ZEBRA_2_EXTERNAL_WEB_PORT=8092
ZEBRA_2_INTERNAL_WEB_PORT=8080

# -----------------------------------------------------------------------------
# MONITORING (Grafana + Prometheus)
# -----------------------------------------------------------------------------
# Monitoring services are in 'monitoring' profile
# Start with: docker-compose --profile monitoring up -d
MONITORING_ENABLED=true

# Prometheus
PROMETHEUS_CONTAINER_NAME=wapro-prometheus
PROMETHEUS_PORT=9090

# Grafana
GRAFANA_CONTAINER_NAME=wapro-grafana
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin123

# -----------------------------------------------------------------------------
# TEST RUNNER
# -----------------------------------------------------------------------------
# Test runner is in 'testing' profile
# Start with: docker-compose --profile testing up
TEST_RUNNER_ENABLED=true
TEST_RUNNER_CONTAINER_NAME=test-runner
TEST_ENVIRONMENT=docker
GENERATE_REPORTS=true

# =============================================================================
# EXAMPLE: Using real ZEBRA printer instead of mock
# =============================================================================
# ZEBRA_1_ENABLED=false
# ZEBRA_1_HOST=192.168.1.50
# ZEBRA_1_SOCKET_PORT=9100
#
# =============================================================================
# EXAMPLE: Using external SQL Server instead of Docker
# =============================================================================
# MSSQL_ENABLED=false
# MSSQL_HOST=192.168.1.100
# MSSQL_PORT=1433
# MSSQL_USER=wapro_user
# MSSQL_PASSWORD=SecurePassword123
# MSSQL_DATABASE=WAPROMAG